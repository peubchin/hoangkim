$(document).ready(function () {    $('.delete_bootbox').on('click', function (e, confirmed) {        var link = $(this).attr('href');        if (!confirmed) {            e.preventDefault();            BootstrapDialog.confirm({                title: 'XÁC NHẬN THÔNG TIN',                message: 'Bạn có thật sự muốn xóa loại sản phẩm này? Nếu đồng ý, tất cả dữ liệu liên quan sẽ bị xóa. Bạn sẽ không thể phục hồi lại chúng sau này!',                type: BootstrapDialog.TYPE_DANGER,                closable: true,                draggable: true,                btnCancelLabel: 'Hủy bỏ',                btnOKLabel: 'Đồng ý',                btnOKClass: 'btn-primary',                callback: function (result) {                    if (result) {                        window.location.href = link;                    }                }            });        }    });    $(".change-inhome").on('ifChanged', function () {        var id = $(this).val();        var strURL = base_url + 'admin/shops/cat/change-inhome';        if ($(this).is(':checked')) {            var form_data = {'id': id, 'value': 1};        } else {            var form_data = {'id': id, 'value': 0};        }        $.ajax({            type: "POST",            url: strURL,            data: form_data,            success: function (html) {                $("#notify").html(html);            }        });        return false;    });		$(".change-is-hide").on('ifChanged', function () {        var id = $(this).val();        var strURL = base_url + 'admin/shops/cat/change-is-hide';        if ($(this).is(':checked')) {            var form_data = {'id': id, 'value': 1};        } else {            var form_data = {'id': id, 'value': 0};        }        $.ajax({            type: "POST",            url: strURL,            data: form_data,            success: function (html) {                $("#notify").html(html);            }        });        return false;    });});