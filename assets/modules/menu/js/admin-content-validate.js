$(document).ready(function () {    $('#position').on('change', function () {        var position = $(this).val();        var strURL = base_url + 'admin/menu/get_menu_position';        var form_data = {'position': position};        $.ajax({            type: "POST",            url: strURL,            data: form_data,            success: function (html) {                $("#parent").html(html);            }        });        return false;    });    $('#menu_type').on('change', function () {        var menu_type = $(this).val();        var strURL = base_url + 'admin/menu/get_menu_type';        var form_data = {'menu_type': menu_type};        $.ajax({            type: "POST",            url: strURL,            data: form_data,            success: function (html) {                $("#content_menu_type").html(html);                $('input[type="checkbox"].flat-blue, input[type="radio"].flat-blue').iCheck({                    checkboxClass: 'icheckbox_flat-blue',                    radioClass: 'iradio_flat-blue'                });            }        });        return false;    });    $("#f-cat").validate({        rules: {            name: {                required: true            }        },        messages: {            name: {                required: 'Nhập tên menu'            }        },        highlight: function (element) {            $(element).closest('.form-group').addClass('has-error');        },        unhighlight: function (element) {            $(element).closest('.form-group').removeClass('has-error');        },        errorElement: 'span',        errorClass: 'help-block',        errorPlacement: function (error, element) {            if (element.parent('.input-group').length) {                error.insertAfter(element.parent());            } else {                error.insertAfter(element);            }        }    });});